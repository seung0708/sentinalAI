import { createClient } from "@/utils/supabase/server";
import { stripe } from "@/app/api/lib/stripe";

//Create a function that randomly creates multiple charges sequentially. 3 times. 
const generateTransactions = async () => {

}